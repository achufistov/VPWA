validate_registration: insecure # (insecure, patched, disabled)
validate_login: patched # (patched, booleansql)
search_posts: unionsql_column # (booleansql_column, booleansql_value, unionsql_column, unionsql_value, jinjassti_value, groovyssti_value, codepenssti_value)
refresh_token: insecure # (insecure, patched)
get_comments_from_post: xss_and_groovyssti # (xss_and_jinjassti, xss_and_groovyssti, xss_and_codepenssti, only_xss)
upload_file: xxe_blind # (xxe, xxe_blind)
